import styled from "@emotion/styled"

const Modal = styled.div({
  position: "absolute",
  top: 0,
  bottom: 0,
  right: 0,
  left: 0,
  backgroundColor: "white",
  height: "100%",
  width: "100%",
})

export default Modal
